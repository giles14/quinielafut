<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<h1> Estamos viendo las Jornadas </h1>
    <table>
        <tr>
            <th>Identificador</th>
            <th>Equipo local</th>
            <th></th>
            <th>Equipo Vistante</th>
            <th></th>
            <th>Resultado</th>        
            {% for objects in jornadas.partidos %}
            <tr>
                <td>{{ objects.identificadorPartido}}</td><td>{{objects.equipoLocal}}</td> <td>{{objects.golesLocal}}</td><td>{{objects.equipoVisitante}}</td> <td>{{objects.golesVisitante}}</td>
                <td>
                    {% if objects.golesLocal > objects.golesVisitante %}
                        Gana local
                    {% elseif objects.golesVisitante > objects.golesLocal %}
                        Gana local
                    {% else %}
                        Empate
                    {% endif %}
                </td> 
            </tr>
            {% endfor %}
        
        
    </table>
</body>
</html>

